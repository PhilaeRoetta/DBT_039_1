models:
  - name: dim_customer
    tests:
      - dbt_utils.equality:
          compare_model: ref('dim_customer_testing_expected')
    columns:
      - name: CUSTOMER_ID
        tests:
          - not_null
  - name: fact_orders
    columns:
      - name: ORDER_ID
        tests:
          - not_null
      - name: order_amount
        tests:
        - positive_values
